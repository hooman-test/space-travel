import {async, ComponentFixture, TestBed} from '@angular/core/testing';

import {LifeFormsComponent} from './life-forms.component';
import {describe, expect} from 'jasmine';

describe('LifeFormsComponent', () => {
/*  let component: LifeFormsComponent;
  let fixture: ComponentFixture<LifeFormsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [LifeFormsComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LifeFormsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
